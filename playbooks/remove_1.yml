---

- hosts: all
  become: true
  vars:
    ansible_python_interpreter: auto_silent
  tasks:

  - name: Remove  apache2 package
    apt:
      name: 
        - apache2
      state: absent

  - name: Remove php package
    apt:
      name:
        - libapache2-mod-php
      state: absent
